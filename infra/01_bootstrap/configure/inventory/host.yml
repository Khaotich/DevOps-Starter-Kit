all:
  children:
    group:
      hosts:
        host:
          ansible_become: true
          ansible_become_method: sudo
          ansible_host: "{{ ip }}"
          ansible_user: "{{ ssh_username }}"
          ansible_ssh_private_key_file: "{{ ssh_private_file_path }}"
          ansible_python_interpreter: "{{ python_interpreter_path }}"